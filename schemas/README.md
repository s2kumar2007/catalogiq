# schemas/

Drop your product schema JSON files here.

Expected files:
- `fasteners.json`   — schema + required fields for fastener products
- `connectors.json`  — schema + required fields for connector products

These files are loaded at runtime by the validation agent (`/api/validate`).
